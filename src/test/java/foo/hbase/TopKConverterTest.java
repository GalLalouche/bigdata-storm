package foo.hbase;

import foo.lift.Lift;
import foo.lift.LiftConverter;
import org.junit.Test;

import static org.junit.Assert.*;

public class TopKConverterTest {
  @Test
  public void name() throws Exception {
//    Lift topK = new Lift(rating, pairs);
//    topK.add(1L, 2.0);
//    topK.add(2L, 5.0);
//
//    LiftConverter $ = new LiftConverter();
//    assertEquals($.fromBytes($.toBytes(topK)), topK);
  }
}
